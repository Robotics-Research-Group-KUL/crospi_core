{
    "$schema":"http://json-schema.org/draft-06/schema",
    "$id":"etasl.json",
    "title":"eTaSL Parameters",
    "type":"object",
    "description":"Parameters needed to run eTaSL",
    "required": ["general", "robotspecification","robotdriver"],
    "properties": {
        "$schema" : {
            "type":"string"
        },
        "general":{
            "$ref":"general.json"
        },
        "initializer":{
            "description": "determines how the feature variables are initialized during initialization of an eTaSL execution",
            "$ref":"initializer.json"
        },
        "solver":{
            "oneOf" : [
                {"$ref":"qpoasessolver.json"}
            ]
        },
        "robotdriver": {
            "title":"Robotdriver",
            "description":"Robotdriver interfaces robot hardware with eTaSL and runs in a separate thread, communicating with shared memory for reduced latency",
            "$ref": "robotdriver.json"
        },
        "robotspecification": {
            "title":"robotspecification",
            "description":"Robot specification that describes the kinematics, jointnames, relevant frames and others",
            "$ref": "robotspecification.json"
        },
        "outputhandlers": {
            "title":"Outputhandlers",
            "description":"Outputhandlers extract data from eTaSL and communicate it in some way to the outside world, such as writing to a file or using a ROS Topic",
            "type" : "array",
            "items" : {
                "$ref" : "outputhandler.json"
            }
        },
        "inputhandlers": {
            "title":"Inputhandlers",
            "description":"Inputhandlers get information from the outside world and puts it into eTaSL",
            "type" : "array",
            "items" : {
                "$ref": "inputhandler.json"
            }
        }
    },
    "additionalProperties":false
}