{
    "$schema": "http://json-schema.org/draft-06/schema",
    "$id": "robotspecification.json",
    "title": "Robot Specification",
    "oneOf": [
        {
            "$ref": "inline_robotspecification.json"
        },
        {
            "$ref":"lua_robotspecification.json"
        },
        {
            "type":"object",
            "properties":{
                "is-emptyrobotspecification" : {
                    "description":"To indicate no robot specification will be loaded before each task specification. Therefore, the robot definition should be loaded at runtime (only at unconfigured states) by the user. Use this option e.g., if different robot specifications are required for different tasks.",
                    "type":"boolean",
                    "const": true        
                }
            },
            "required":["is-emptyrobotspecification"],
            "additionalProperties": false
        }
    ]
}
