{
    "$id": "moveto.json",
    "title": "move-to",
    "description": "Moves to a given location in space",
    "type": "object",
    "required": [
        "target",
        "maxacc"
    ],
    "additionalProperties": false,
    "properties": {
        "$schema": {
            "type": "string"
        },
        "target": {
            "$ref": "frame.json"
        },
        "maxvel": {
            "description": "maximum velocity allowed during the motion",
            "type": "number",
            "minimum": 0.0,
            "default": 1.0
        },
        "maxacc": {
            "description": "maximum acceleration allowed during the motion",
            "type": "number",
            "minimum": 0.0,
            "default": 0.0
        },
        "etaslfile": {
            "description": "etaslfile that defines the task to be executed, this file will read in the other parameters",
            "type": "string",
            "interpolate": true
        }
    }
}